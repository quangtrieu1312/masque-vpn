services:
  masque-server:
    build: .
    cap_add:
      - NET_ADMIN
      - NET_RAW
    privileged: true
    devices:
      - /dev/net/tun:/dev/net/tun
    ports:
      - 443:443/udp
    volumes:
      - ./certs:/opt/masqued/certs
      - ./ca:/opt/masqued/ca
    command: /run.sh
